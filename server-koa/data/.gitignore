# Working IIFE files are decrypted at startup from data/mock/*.enc.json
# Edit them freely; they are re-encrypted to data/mock on server shutdown.
*.iife.js
!*.init.iife.js

# Runtime / generated JSON (enc.json lives in data/mock/, not here)
*.json
