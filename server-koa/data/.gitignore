# Working IIFE files are decrypted at startup from data/enc/*.enc.json
# Edit them freely; they are re-encrypted to data/enc on server shutdown.
*.iife.js
!*.init.iife.js

# Runtime / generated JSON (enc.json lives in data/enc/, not here)
*.json
