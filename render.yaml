services:
  - type: static_site
    name: react-seed
    rootDir: seed
    buildCommand: npm ci && npm run build:render
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    autoDeploy: true
